services:
  # ── Monolithic App (FastAPI Backend + React Frontend) ───────────────
  - type: web
    name: docmind-rag-chatbot
    env: docker
    dockerfilePath: ./Dockerfile
    region: oregon # or whichever region you prefer
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false          # ← Set this manually in Render dashboard
